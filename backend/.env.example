# Copy to backend/.env to override defaults. Relative SQLite paths are
# resolved against the backend/ directory.
DATABASE_URL=sqlite:///./sugar.db
JWT_SECRET=change-me-in-production-use-a-long-random-secret
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=10080
